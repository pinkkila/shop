spring:
  docker:
    compose:
      skip:
        in-tests: false
      file: ./../../compose.yaml

  datasource:
    username: myuser
    password: secret
    url: "jdbc:postgresql://127.0.0.1:5432/shop_db"

  sql:
    init:
      mode: always